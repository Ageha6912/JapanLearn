# 日语学习 Android App MVP PRD

## 1. 文档信息

- **产品名称**：日语学习 App（暂定）
- **文档类型**：MVP 产品需求文档
- **目标平台**：Android
- **当前版本**：MVP v0.1
- **产品定位**：面向日语初学者，提供“学习 → 练习 → 复习 → 进步”的轻量化学习闭环。

---

## 2. 产品目标

### 2.1 核心目标

第一阶段不追求做成完整的日语学习平台，而是验证以下核心假设：

> 用户是否愿意通过“每日学习 + 即时练习 + 间隔复习”的方式持续学习日语。

### 2.2 MVP 成功标准

重点关注：

- 用户能否顺利完成第一次学习
- 用户是否愿意每天打开 App
- 用户是否能够完成学习 → 复习闭环
- 用户是否能够明显感受到学习进度
- 单词/语法学习流程是否简单易懂

---

## 3. 目标用户

### 核心用户

- 日语零基础或初级学习者
- 正在学习 JLPT N5/N4 的用户
- 希望利用碎片时间学习日语的用户
- 喜欢通过短任务持续学习的用户

### 暂不重点覆盖

- JLPT N1/N2 高级用户
- 系统性备考用户
- 需要完整口语/听力课程的专业学习者

---

## 4. 产品核心闭环

```text
今日学习
   ↓
学习单词 / 语法
   ↓
即时练习
   ↓
记录掌握程度
   ↓
SRS 间隔复习
   ↓
查看学习进度
   ↓
第二天继续学习
```

---

# 5. 功能范围

## 5.1 MVP 功能总览

| 功能 | MVP |
|---|---|
| 用户系统 | 🟡 游客本地模式（登录/云同步推迟，见 §17） |
| 五十音 | ✅ |
| N5 单词 | ✅ |
| N5 基础语法 | ✅ |
| 单词音频 | ✅ |
| 单词练习 | ✅ |
| 语法练习 | ✅ |
| SRS 复习 | ✅ |
| 今日学习 | ✅ |
| 学习统计 | ✅ |
| 每日一句 | ✅ |
| AI 日语助手 | ❌ 移出 MVP（见 §17） |
| 汉字专项学习 | ❌ |
| 专项听力 | ❌ |
| 口语训练 | ❌ |
| AI 实时对话 | ❌ |
| 社交系统 | ❌ |
| 排行榜 | ❌ |
| 商城 / 虚拟货币 | ❌ |
| 社区 | ❌ |

---

# 6. 页面结构

建议 MVP 采用底部 4 Tab：

```text
首页
├── 今日学习
├── 今日复习
├── 学习进度
└── 每日一句

学习
├── 五十音
├── N5 单词
└── N5 语法

复习
├── 今日复习
├── 单词复习
├── 语法复习
└── 错题

我的
├── 学习统计
├── 学习目标
├── 设置
└── AI 日语助手
```

---

# 7. 功能需求

## 7.1 用户系统

### 功能

- 注册 / 登录
- 游客模式（可选）
- 用户学习数据同步

### MVP 建议

如果希望尽快验证产品，可以优先支持：

- Email 登录
- Google 登录
- 游客本地学习

用户登录后同步学习记录。

---

## 7.2 首页 / 今日学习

### 目标

让用户打开 App 后能够立即知道：

> 今天应该学什么。

### 页面内容

- 欢迎语
- 今日学习任务
- 今日新词数量
- 今日待复习数量
- 今日学习进度
- 连续学习天数
- 开始学习按钮
- 每日一句

### 示例

```text
こんにちは！

今日学习

新词       20
待复习     35
语法        5

今日进度
███████░░░ 70%

[ 开始今日学习 ]

🔥 连续学习 7 天
```

---

## 7.3 五十音

### 功能

提供平假名、片假名基础学习。

### 页面

```text
あ  い  う  え  お
か  き  く  け  こ
さ  し  す  せ  そ
...
```

### 点击假名

显示：

- 假名
- 罗马音
- 发音
- 示例单词

例如：

```text
あ

a

🔊

あさ
朝
早晨
```

### 练习

支持简单选择题：

```text
「き」怎么读？

A. ki
B. ka
C. ku
D. ke
```

---

## 7.4 单词学习

### 内容

每个单词包含：

- 日文
- 假名
- 中文释义
- 词性
- 音频
- 示例句
- 示例句翻译

### 示例

```text
食べる

たべる

吃

🔊

毎日ご飯を食べます。
每天吃饭。
```

### 学习流程

```text
展示单词
 ↓
查看释义
 ↓
播放音频
 ↓
完成练习
 ↓
选择掌握程度
 ↓
进入下一项
```

### 掌握程度

- 不认识
- 模糊
- 熟悉

掌握程度用于后续 SRS 复习。

---

## 7.5 单词练习

MVP 优先支持选择题和简单回忆题。

### 类型

#### 日语 → 中文

```text
「食べる」是什么意思？

A. 喝
B. 吃
C. 看
D. 买
```

#### 中文 → 日语

```text
“吃”对应哪个词？

A. 飲む
B. 見る
C. 食べる
D. 買う
```

后续可增加：

- 听音选词
- 拼写
- 看假名选汉字

---

## 7.6 语法学习

### MVP 范围

优先提供 N5 基础语法。

### 每条语法结构

```text
语法名称
↓
含义
↓
接续方式
↓
使用说明
↓
例句
↓
练习
```

### 示例

```text
～です

含义：
是……

接续：
名词 + です

例句：

私は学生です。
我是学生。

これは本です。
这是书。
```

### 练习

```text
私は学生＿＿。

A. です
B. ます
C. でした
D. ません
```

---

## 7.7 SRS 复习系统

### 目标

根据用户学习表现，自动安排后续复习。

### 基础规则

MVP 采用简单规则，不需要一开始实现复杂算法。

| 掌握程度 | 调度行为 |
|---|---|
| 不认识 | 本次会话内重新出队 + 记入错题本（不设真实 10 分钟定时器） |
| 模糊 | 1 天后复习 |
| 熟悉 | max(3 天, 上次间隔 × 1.5) |
| 熟练 | max(7 天, 上次间隔 × 2)，间隔上限 60 天 |

后续可以升级为 FSRS 等更成熟的算法。

### 今日复习

```text
今日复习

单词     25
语法      5
错题      5

共 35 项

[ 开始复习 ]
```

---

## 7.8 每日一句

### 产品定位

通过真实场景让用户学习能够立即使用的日语。

### 示例

```text
今日一句

ちょっと待ってください。

请稍等一下。

🔊

ちょっと
一点 / 稍微

待って
等待

ください
请……
```

### 场景分类

- 日常聊天
- 餐厅
- 便利店
- 旅游
- 学校
- 工作
- 动漫 / 娱乐

### 学习流程

```text
查看句子
 ↓
播放发音
 ↓
查看拆解
 ↓
完成一句练习
```

---

## 7.9 学习统计

### MVP 数据

- 连续学习天数
- 累计学习时间
- 学习单词数量
- 掌握单词数量
- 完成课程数量
- 每日学习记录

### 周统计

```text
本周学习

周一 ████
周二 █████
周三 ███
周四 ██████
周五 ████
周六 █████
周日 ██
```

重点是让用户看到自己的成长，而不是制作复杂的数据分析系统。

---

## 7.10 AI 日语助手

### MVP 定位

AI 作为辅助功能，而不是整个 App 的核心。

### MVP 支持

#### 1. 翻译 / 生成

用户：

```text
我想说“昨天和朋友去了东京”
```

AI：

```text
昨日、友達と東京に行きました。
```

#### 2. 语法解释

用户：

```text
为什么这里用「行きました」？
```

AI：

```text
因为句子描述的是昨天发生的事情，
所以「行く」需要使用过去式「行きました」。
```

#### 3. 句子纠错

用户：

```text
昨日私は友達と東京へ行く。
```

AI：

```text
更自然：

昨日、友達と東京に行きました。

因为“昨日”表示过去时间，
所以使用过去式。
```

### 暂不实现

- 实时语音 AI 对话
- AI 角色系统
- AI 长期记忆
- AI 口语评分
- AI 全自动课程生成

---

# 8. 学习内容规划

## MVP 首期内容

### 五十音

- 平假名
- 片假名
- 基础发音

### N5 单词

建议首期：

**300～500 个单词**

覆盖：

- 人物
- 数字
- 时间
- 食物
- 地点
- 日常动作
- 常用形容词
- 常用副词

### N5 语法

建议首期：

**30～50 条基础语法**

例如：

- ～です
- ～ます
- ～ません
- ～でした
- ～ではありません
- は
- が
- を
- に
- で
- と
- の
- も
- から
- まで
- これ / それ / あれ
- ここ / そこ / あそこ

---

# 9. 数据模型

MVP 可以围绕以下核心实体设计：

```text
User
 ├── UserProgress
 ├── ReviewRecord
 └── DailyStudy

Course
 ├── Lesson
 │    ├── Word
 │    ├── Grammar
 │    └── Exercise
 │
 └── Example
```

### 核心数据

#### User

```text
id
email
createdAt
learningGoal
dailyTarget
```

#### Word

```text
id
japanese
kana
meaning
partOfSpeech
audioUrl
example
exampleTranslation
level
```

#### Grammar

```text
id
title
meaning
explanation
connection
examples
level
```

#### UserProgress

```text
userId
contentId
contentType
status
mastery
nextReviewAt
reviewCount
```

#### ReviewRecord

```text
id
userId
contentId
result
reviewedAt
nextReviewAt
```

---

# 10. 技术方案建议

## Android

推荐：

- Kotlin
- Jetpack Compose
- Android Jetpack
- Navigation Compose
- Room
- Kotlin Coroutines
- ViewModel

架构可以采用：

```text
UI
 ↓
ViewModel
 ↓
UseCase / Repository
 ↓
Room / API
```

MVP 阶段不需要过度追求复杂的 Clean Architecture。

## 后端

如果目标是快速开发：

- Supabase / Firebase

如果希望完全掌控后端：

- Node.js / NestJS 或 Spring Boot
- PostgreSQL

## AI

建议：

```text
Android
   ↓
自己的后端 API
   ↓
LLM API
```

不要将 AI API Key 直接放在 Android 客户端。

## 音频

建议：

- 预生成日语发音
- CDN / 对象存储
- Android 本地缓存

避免每次播放都实时调用 TTS API。

---

# 11. MVP 页面优先级

## P0：必须完成

1. 首页
2. 五十音
3. 单词学习
4. 单词练习
5. 语法学习
6. 语法练习
7. SRS 复习
8. 今日学习
9. 基础学习统计

## P1：建议完成

1. 每日一句
2. 错题
3. 用户登录 / 数据同步
4. 学习目标

## P2：后续版本

1. AI 日语助手
2. 听力训练
3. 汉字专项
4. 口语训练
5. AI 对话
6. 更复杂的学习分析

---

# 12. 非功能需求

## 性能

- 首页打开尽量控制在 2 秒内
- 学习内容优先使用本地缓存
- 音频播放应尽量无明显延迟
- 学习过程中尽量避免频繁网络请求

## 离线

MVP 建议支持基础离线学习：

- 五十音
- 已下载单词
- 已下载语法
- 基础练习
- SRS 本地记录

联网后再同步数据。

## 数据安全

- API Key 不放客户端
- 用户学习数据需要进行身份隔离
- 后端接口进行鉴权
- 用户数据定期同步

---

# 13. MVP 暂不考虑

为了控制开发成本，以下功能不进入首版：

- 社交 / 好友
- 排行榜
- 社区
- 商城
- 虚拟货币
- 复杂签到奖励
- 视频课程
- OCR 拍照识词
- 手写识别
- 实时语音对话
- 高级 AI 老师
- N1/N2/N3 内容
- 复杂汉字系统

---

# 14. MVP 开发阶段

## Phase 1：基础框架

- Android 项目初始化
- Compose UI
- Navigation
- Room
- 基础数据模型
- 本地课程数据

## Phase 2：核心学习

- 五十音
- 单词
- 单词练习
- 语法
- 语法练习

## Phase 3：复习系统

- 用户学习记录
- 掌握程度
- SRS
- 今日复习
- 错题

## Phase 4：产品化

- 首页
- 学习统计
- 每日一句
- 登录 / 同步
- 设置

## Phase 5：AI

- AI 日语助手
- 翻译
- 语法解释
- 句子纠错

---

# 15. MVP 核心原则

### 原则 1：先保证学习闭环

不要为了功能数量牺牲学习体验。

### 原则 2：内容比功能更重要

500 个高质量单词 + 30 条高质量语法，比 20 个复杂功能更重要。

### 原则 3：尽量降低每日学习成本

目标是用户打开 App 后，可以在 **5～15 分钟**内完成一次学习。

### 原则 4：让用户看到进步

学习统计、掌握数量、连续学习等反馈应该清晰直观。

### 原则 5：AI 是增强项

AI 用于解决用户具体问题，而不是替代基础课程和复习系统。

---

# 16. MVP 最终形态

第一版最终应该能够让一个日语初学者完成：

```text
注册 / 进入 App
      ↓
学习五十音
      ↓
开始 N5 学习
      ↓
学习 20 个单词
      ↓
学习 1～3 条语法
      ↓
完成练习
      ↓
记录掌握程度
      ↓
系统安排复习
      ↓
第二天打开 App
      ↓
复习昨天内容
      ↓
学习新的内容
      ↓
查看自己的学习进度
```

**MVP 的核心不是“功能多”，而是把这个循环做得足够顺畅，让用户愿意每天回来。**

---

# 17. 评审决策记录（v0.2）

以下为 PRD 评审后确认的修订，**效力等同需求**，与正文冲突处以本节为准。

## 17.1 架构决策：MVP 完全本地化

- **不接入任何后端**（Supabase/Firebase 均推迟）。课程内容以版本化 JSON 打包进 APK assets，学习记录存 Room。
- 登录 / 数据同步整体推迟（正文 §5 总览表中“用户系统”降级为游客本地模式）。
- Room 采用本地优先设计，后续接后端同步时增量迁移。

## 17.2 范围修订

- **AI 日语助手移出 MVP**（正文 §5 标记同步修订）：需要后端 + API Key 管理，对验证核心闭环贡献最低，移入 §11 P2。
- 每日一句保留在 MVP（P1 → 实现），错题本保留（P1 → 实现）。
- 正文 §11 P0 清单全部实现。

## 17.3 每日任务量校准

正文 §7.2 示例“新词 20 + 待复习 35 + 语法 5”与 §15 原则 3（5~15 分钟）冲突，修订为：

- 每日新词默认 **10 个**，用户可在 5/10/15/20 之间调整。
- 每日新语法默认 **3 条**。
- 每日复习上限 **30 条**（SRS 到期量会滚动累积，必须限流，超出部分顺延次日）。

## 17.4 音频方案修订

原建议“预生成音频 + CDN”需内容生产管线，MVP 改为：

- **使用 Android 系统 TTS（TextToSpeech，ja-JP）**，零成本、离线可用。
- 后续版本再评估预生成音频 + 本地缓存的方案。

## 17.5 SRS 简化

- “不认识 → 10 分钟”改为**本次会话内重新出队 + 记入错题本**（正文 §7.7 表格已同步修订），避免真实定时调度。
- 调度器为纯函数领域对象，先于 UI 实现，FSRS 为后续演进方向。

## 17.6 内容生产管线（与 Phase 1 并行）

- 定 义 JSON Schema：`kana.json` / `words_n5.json` / `grammar_n5.json` / `sentences.json`，带 `version` 字段，App 首次启动按版本装载入库。
- 首期内容量：五十音 46 组（平/片对照）、N5 单词约 200 个（带例句）、N5 语法 30 条（每条带例句与练习题）、每日一句 30 条（带词汇拆解）。

## 17.7 验证指标与埋点

正文 §2.2 定性标准量化为：

| 指标 | 目标 |
|---|---|
| 首次启动完成首个学习会话 | ≥ 60% |
| D1 留存（次日完成复习） | ≥ 30% |
| D7 留存 | ≥ 15% |
| 今日学习完成率 | ≥ 70% |

- MVP 无后端，以上指标先以本地统计呈现（统计页）；正式内测时接入 Firebase Analytics（预留接入点，不阻塞 MVP）。

## 17.8 测试策略（强制）

所有改动必须附带测试且全部通过后才能交付。MVP 单元测试覆盖：

- SRS 调度（四级掌握度 → 间隔/到期时间、间隔递增、上限）
- 练习题生成（选项数量、含正确答案、不重复、方向 JP→CN / CN→JP）
- 连续学习天数计算（跨天、中断、当天）
- 每日复习限流（到期量 > 上限时截断，余量顺延）
- 内容 JSON 解析（schema 校验、字段完整性）

## 17.9 技术栈锁定

- Kotlin 2.0 + Jetpack Compose（Material3）+ Navigation Compose + Room（KSP）+ kotlinx-serialization + Coroutines/ViewModel
- minSdk 26 / targetSdk 35 / compileSdk 35，JDK 17
- 手工依赖注入（AppContainer），不引入 Hilt（控制 MVP 复杂度）

---

# 18. v0.2.0 决策记录

v0.1 之后、正式版 v0.2.0 的规划结论（拷问式评审产出），与正文冲突处以本节为准。

## 18.1 版本定位

- 首要目标：让作者与身边人**真实日常使用**，围绕留存做功能；分发渠道为 **GitHub Releases（正式签名 APK）**。
- 不写任何后端代码：登录/云同步与 AI 助手继续推迟到 v0.3+。

## 18.2 内容规模（原 §17.6 目标达成）

- 单词 200 → **504**（11 个分类，每词带假名/词性/例句）
- 五十音 46 → **101**：清音 46 / 浊音 25（含半浊音）/ 拗音 30（标准拗音表，不含ぱ行拗音）
- 语法 30 → **50**
- 新增内容校验管线 `tools/validate_content.py`：假名/罗马音规范、重复词条、schema 完整性检查；扩充脚本与批次词库保留在 `tools/` 供后续复用

## 18.3 新增功能

- **每日复习提醒**：WorkManager 每日 20:00 触发，有到期内容才通知，点击深链到复习 Tab；设置页开关 + Android 13+ 运行时权限；时间自定义推迟
- **听音选词题型**：新词/复习会话中「日语→中文」题以 30% 概率升级为 TTS 听音变体（触发策略为纯函数 `AudioQuizPolicy`）
- **五十音三分组**：列表与测验均按清音/浊音/拗音分组，测验题量 10/20/全部可选
- **单词列表掌握度色点**：未学空心 / 不认识灰 / 模糊黄 / 熟悉抹茶 / 熟练藍
- 四个 Tab 标题固定不随内容滚动

## 18.4 发布工程

- R8 minify + 资源收缩开启（APK 16.6MB debug → 1.5MB release），kotlinx-serialization keep 规则
- 正式签名：项目根目录 keystore（gitignore 覆盖，已验证从未进入 git 历史）；此后 Releases 均发正式签名 APK
- GitHub Actions 门禁 CI：push/PR 自动跑全部单元测试 + assembleDebug；keystore 不上云
- FSRS、登录同步、AI 助手继续推迟；待真实使用数据后再评估

---

# 19. v0.5–v0.7 决策记录与产品缺口清单

> 依据 `OPTIMIZATION.md`（Accepted，2026-09-09 确认 Q1–Q3）写入，工程结论自 v0.5.0 起逐版落地；19.5 为 2026-09-12 缺口盘点。本节效力等同需求，与正文冲突处以本节为准。

## 19.1 内容装载与工程债（v0.5.0 交付，v0.7.5 收尾）

- 内容版本由四文件 `version` **加总**改为**每文件独立 meta key**（`content_version_kana/words/grammar/sentences`）；一次启动的全部装载包在**一个** Room `withTransaction` 中，中途崩溃不会留下半新半旧的表。
- 装载时删除资产中已不存在的 id（incoming 必须等于文件全量）；**orphan 进度保留**（Q1/D3，2026-09-09 确认）：`user_progress` / `wrong_answers` / `review_records` 不级联删除，已学/到期计数 JOIN 内容表。
- 首页/统计计数改 COUNT Flow + JOIN，不再为取 `.size` 收集内容全表。
- 版本 key 双写过渡（D11）：v0.5.0 同时写新 key 与旧加总 key 以保回滚；v0.7.5 停写并删除旧加总 key `content_version`（仅保留读取，用于 0.4.x 升级判定与四文件全量重装）。
- Room `exportSchema = true`，schema JSON 入库 `app/schemas/`；迁移测试走 JVM，CI 不上模拟器。

## 19.2 练习题型与内容（v0.5.0 / v0.6.1 交付）

- **产出型题（打假名）**：中→日方向约 20% 触发，键入假名作答；罗马音标准为 Modified Hepburn，答案同时接受假名与词库 `romaji.lowercase()`；不做手写、不强依赖日语 IME。
- **智能干扰项**：分桶取样、桶内 shuffle——先耗尽同 `pos`，再同 `cat`，再其余；汉字题干扰项限定汉字形并排除同音（沿用）。`QuizWord` 增补 `pos`/`cat`，`QuizVariantPicker` 统一选择/听音/汉字/打字变体。
- **内容**：N4 语法 +7 条（不与现有 title 冲突）、每日一句 60 → 120。
- **学习 UX**（兑现 §18.3 推迟项）：单词搜索与筛选、复习提醒时刻可调、五十音横幅。

## 19.3 SRS → FSRS（v0.7.0 交付）

- 先抽 `Scheduler` 接口保持行为不变，再 vendor 精简移植 `ts-fsrs` v5.4.2（long-term，`enable_short_term = false`、`enable_fuzz = false`）；持久化列名用 `fsrsState`（避开 `status`）。
- 自评按钮文案**保持**「不认识 / 模糊 / 熟悉 / 熟练」（Q3/D8，2026-09-09 确认），仅内部映射 Again/Hard/Good/Easy。
- 不变式：**不认识 → `dueAt = now`**；已掌握判定 `stability >= 21`；Room v3 → v4（`MIGRATION_3_4` 不得删除）。
- 迁移不改已有 `dueAt`，新算法自下次自评起生效。

## 19.4 发音（Q2/D7，2026-09-09 确认）

- v0.5–v0.7 继续系统 TTS，**不为预生成音频增大 APK**。
- 预生成音频排在 v0.8，仍是**可选项**：默认不打进 APK、不在默认路径上；门禁为无书面授权的声库不得合入。
- 真机事实沉淀（v0.7.1–v0.7.5）：中文 TTS 引擎会谎报日语能力（只念汉字跳过假名）；非 Google TTS 一律判日语不可用并引导安装；仅信任本地已安装的 ja voice；支持音色选择与切换。

## 19.5 产品缺口清单与优先级（2026-09-12 确认）

作为 v0.8+ 版本规划的输入，每次规划从本清单取项，完成即勾选。低优先级项与 §17.1（完全本地化、登录同步推迟）、§17.2（AI 移出 MVP）一致，不改变既有推迟决策。

### 高优先级

- [x] 新用户 Onboarding（v0.8.0 已交付）
- [x] N5/N4 课程化（v1.0.0 已交付）
- [x] 个性化每日学习计划（v0.8.0 已交付）
- [ ] 内容扩充（常驻并行线；v0.9 已扩每日一句至 180，见 19.7）
- [x] 完整学习目标系统（v0.8.0 已交付）
- [x] 完整听力训练（v0.9.0 已交付）

### 中优先级

- [ ] 错题与 SRS 深度联动
- [ ] 汉字专项学习
- [ ] 更完整学习统计
- [ ] 匿名埋点 / 数据分析（接入点预留见 §17.7）
- [x] 学习成果页（v1.0.0 已交付）
- [ ] 更丰富的即时反馈

### 低优先级 / v1.0+

- [ ] AI 语法助手（正文 §7.10，**v1.1.0 已交付 BYOK 形态**，见 19.9）
- [ ] AI 句子纠错（**已并入 v1.1.0 助手页三模式之一**）
- [ ] 口语跟读
- [ ] 发音评分
- [ ] 登录（§17.1：继续游客本地模式）
- [ ] 云同步（同上）
- [ ] 远程内容更新
- [ ] JLPT 专项课程

### 目前完全可以不做

- 社区、好友、排行榜、商城、虚拟货币

## 19.6 v0.8 规划决策（2026-09-12 grilling 定案）

grilling 两轮问答确认，效力等同需求。后续实现如需偏离本节，先改本节再动代码。

### 主题与范围

- **主题**：学习目标系统 + 个性化每日学习计划（兑现 §11 P1「学习目标」欠账）。**听力训练留 v0.9** 单独做主题。
- **目标形态**：级别（N5/N4）+ 可选目标日期。有日期按剩余内容量线性倒推所需节奏，无日期按默认节奏；进度度量用内容表存量（N5 504 词 + 50 语法 / N4 300 词 + 30 语法），五十音视为入门前置、不计入目标。
- **计划算法**：倒推结果夹在现有档位 5/10/15/20 内自动推荐一档，用户可手动覆盖；落后进度**按周校准**（每周重算推荐档，不做每日追赶施压）；复习上限 30、语法每日 3 **不变**（§17.3）。实现为纯函数 `StudyPlanner`（单元测试强制），`ReviewPlanner` 与 FSRS 零接触。
- **数据落点**：目标状态存 SharedPreferences（`SettingsRepository`），**不新增 Room 表、不动 schema**。
- **UI**：我的 Tab 新增「学习目标」页（固定头部 + 独立滚动区模式）：选级别、设/清目标日期、进度条与推荐档；首页任务卡加**只读**摘要行（「距 N5 目标 N 天 · 已完成 x%」），点击跳目标页。

### Onboarding（搭车件）

- 首启三步引导，随时可跳过：① 一屏说明「学习 → 练习 → 复习」闭环 → ② 选当前水平并顺带收集目标（复用目标页数据写入）→ ③ 直达五十音或单词学习。
- 完成写 `onboardingDone` 标记；跳过者与已有进度的老用户**永不打扰**；零进度用户首页任务卡追加「先学五十音」轻引导横幅。
- 独立 PR，不阻塞主题。

### PR 切分与版本

- **PR-A**：目标系统 + 个性化计划（`StudyPlanner` 纯函数 + 目标页 + 首页摘要行）
- **PR-B**：Onboarding 三步引导（可先行合入）
- **PR-C**：收口发版 PR（versionName 0.8.0 / versionCode 19 + Release Notes）
- PR-B 赶不上收口则随 v0.8.1 发，不阻塞主题。每个 PR 照旧测试全绿交付（§17.8）。

### 版本分配结论

- **预生成音频正式移出 v0.8** → 无排期的可选 PR，门禁仍为声库书面授权（见 19.4）。
- **匿名埋点继续推迟**至正式内测（§17.7）。
- **内容扩充为常驻并行线**：随批次扩 N4 词/语法/每日一句，不占主题、不挡发版。

## 19.7 v0.9 规划决策（2026-09-12 grilling 定案）

grilling 两轮问答确认，效力等同需求。后续实现如需偏离本节，先改本节再动代码。

### 主题与题型

- **主题**：完整听力训练（§11 P2 列名 + 19.5 高优先级）。发音基础继续系统 TTS（v0.7.x 引导链复用）；预生成音频维持无排期可选 PR（19.4 不变）。
- **三题型**：
  1. **听音辨词**：播单词音频 → 四选一中文释义（现有 `AudioQuizPolicy` 题型独立化）
  2. **听写假名**：播单词音频 → 键入假名（复用打字题 `TypeAnswerNormalizer`，假名/罗马音均判对）
  3. **听句选义**：播整句音频 → 三选一中文意思（素材为每日一句库）
- **不做**跟读与发音评分（19.5 低优先级，依赖录音与评分引擎）。

### 入口与进度边界

- 入口：学习 Tab 新增「听力训练」入口卡 → 独立会话页，题量 5/10/20；**不新增底部 Tab**，四 Tab 结构不动。
- 听力会话**逐题落统计**（`stats.addStudy`），**答错的词进错题本**；但**不推进 SRS 调度**——听力是辅练，不替用户「复习掉」到期卡片。「错题与 SRS 深度联动」由此推进半步（错题多一个来源），完整联动留后续。

### 题源与配比

- **听音辨词**：该级别全池（按 `studyLevel`），已学词优先排前。
- **听写假名**：**仅从已学词**出题；已学词不足 5 个时该题型回退为听音辨词。
- **听句选义**：句子全库轮抽，不依赖学习进度。
- 会话固定混合配比 **40 / 30 / 30**，听写回退时比例就近匀给听音辨词；**不设每日限流、不进首页任务卡**；练习时长与题数照常进统计页。

### 会话交互

- 每题音频**进题自动播一次** + 题面常驻「重播」按钮（复用 `JapaneseTts`）。
- 会话开始时 TTS 不可用 → 直接走 v0.7.x 引导链（装 Google TTS / 下载语音数据），不让用户面对哑巴题。
- 听写题调起假名键盘，接受假名与罗马音；会话结算复用现有会话结算页。

### 搭车件与 PR 切分

- **每日一句 120 → 180**（+60，场景补齐购物/交通/就医等），走既有 `tools/` 批次管线 + `validate_content.py` 校验 + 内容 version 升位。
- **PR-A**：听力训练（`domain/ListeningQuizGenerator.kt` 纯函数——题型配比、题源选择、干扰项，全部带单元测试；听力会话 ViewModel + 会话页；学习 Tab 入口卡）
- **PR-B**：每日一句 120 → 180
- **PR-C**：收口 v0.9.0（versionName 0.9.0 / versionCode 20 + Release Notes + 模拟器回归 + tag + GitHub Release）
- 顺序 PR-A → PR-B → PR-C；PR-B 素材生产延迟不阻塞 PR-A 合入。每个 PR 照旧测试全绿交付（§17.8）。

## 19.8 v1.0 规划决策（2026-09-12 grilling 定案）

grilling 两轮问答确认，效力等同需求。后续实现如需偏离本节，先改本节再动代码。

### 主题与搭车件

- **主题**：N5/N4 课程化（19.5 高优先级收尾）——从「内容列表」升级为「有序课程单元」。
- **搭车**：学习成果页（我的 Tab）；收口 PR 带 **README 里程碑重写** + showcase 截图重生成。
- **埋点与分发维持现状**：继续推迟至正式内测（§17.7 / 19.6 决策不变）。

### 课程化设计

- **单元划分（分类聚类）**：现有 11 个分类即单元骨架，友好命名（如「人物与称呼」「数字与时间」），N5 / N4 各 11 个单元，粒度 19–74 词；语法按现有难度顺序均分到单元（N5 每单元约 4–5 条）。不做人工场景重排（可作为未来内容批次方向）。
- **解锁（软推进）**：浏览永远自由；「当前单元」指针 = 第一个未完成单元（完成 = 该单元词全部学过，即进过 SRS）；**今日学习的新词改为从当前单元顺序取**，替代全池随机；指针可手动切换；老用户已有进度按内容 id 关联计入单元完成度，不被锁。
- **单元检查点（soft）**：单元详情页「单元测试」10 题，从该单元词出混合题型（复用 `QuizVariantPicker`，可选听力）；成绩落统计 + 错题本，不强制通过。
- **数据方案**：`words.json` / `grammar.json` 加 `unit` 字段（1–11），Room **v4 → v5** 迁移给两表加 `unit` 列（沿 `level` 列 v2→v3 先例）；内容 version 升位自动重装，orphan 进度策略不变（19.1）。
- **UI**：学习 Tab「单词」「语法」两卡合并为一张「课程」大卡（当前单元名 + 进度条）→ 单元列表页（22 个单元卡：进度/完成态/检查点成绩）→ 单元详情页（该单元词表 + 语法 + 单元测试按钮）；全量单词列表保留为「全部单词」入口并加单元筛选；错题本等不受影响。复习/SRS 完全不动（19.6 纪律）。

### 学习成果页

- 我的 Tab 新增「学习成果」：累计学习时长、已学/掌握单词数、当前与最长连击、单元完成 x/22、听力与练习累计正确率、五十音进度；全部来自现有表，零新表纯聚合。

### PR 切分与版本

- **PR-A**：数据与迁移（unit 归属脚本 + JSON/Room v5 + validate + 单测）
- **PR-B**：课程 UI（单元列表 / 详情 / 今日取词改造 / 检查点）——主题主体
- **PR-C**：学习成果页（可与 PR-B 并行）
- **PR-D**：收口 v1.0.0（versionName 1.0.0 / versionCode 21 + README 里程碑 + showcase 重生成 + Release）
- 顺序 A → B → C → D，README 只在 D 动。每个 PR 照旧测试全绿交付（§17.8）。

### 版本分配结论

- **AI 助手（语法解释 / 句子纠错，正文 §7.10）v1.0 不做**。建议形态为**用户自备 API Key（BYOK）直连 OpenAI 兼容端点**：不建自有后端、不加登录，保住 §17.1 完全本地化不变式；排期为 **v1.1 候选**，触发条件为 v1.0 发布后留存稳定或作者主动提出，下轮规划最终拍板。

## 19.9 v1.1 规划决策（2026-09-12 grilling 定案）

grilling 两轮问答确认，效力等同需求。后续实现如需偏离本节，先改本节再动代码。

### 离线不变式修订（对 §17.1）

- §17.1「不接入任何后端」自 v1.1 起修订为：**核心学习闭环保持完全离线、零账号；AI 助手是唯一可选的在线增强**。
  - 不填 API Key 则功能完全不存在（相关入口全部隐藏）；
  - 请求只包含用户主动输入或点选的文本内容，**任何学习数据都不出设备**；
  - AI 页面与设置页明确标注「需联网，调用费用由用户的 API Key 承担」。

### 主题与范围

- **主题**：AI 语法助手（BYOK——用户自备 API Key，App 直连第三方 LLM，不建自有后端、不加登录）。
- **功能**：AI 助手页三模式——**语法解释 / 句子纠错 / 翻译**（三套预置 Prompt 共用一条请求管线，输出纯文本 + 简单排版）。
- **上下文入口**：语法详情页「问 AI 讲解」（带入该条语法标题与例句）、错题本条目「AI 讲解」——跳转助手页并预填输入框，可修改可直接发送。
- **不做**：语音对话 / 口语评分（19.5 低优先级）、流式打字机输出（留 v1.2 增强）、登录与云同步（继续推迟）。

### Provider 与配置

- 兼容一切 OpenAI `/chat/completions` 格式：设置页「AI 助手」配置区三字段——**接口地址（Base URL）+ API Key + 模型名**。
- 内置三个预设模板一键填入：**DeepSeek / 智谱 GLM / OpenAI**；三字段均可改，Ollama 等本地端点同样适用。

### Key 存储与隐私

- Key 存本地 SharedPreferences（明文），输入框密码样式遮蔽；**不进备份导出、不打日志**。
- 助手页与设置页各放一句隐私说明：「Key 仅保存在本机，请求只包含你主动输入的内容，不发送任何学习数据」。

### 费用保护与交互

- **每日调用上限默认 20 次/天**，设置可选 10/20/50/不限；按日期本地计数（SharedPreferences），超限时提示次日再来或调高上限。
- 响应**一次性返回 + 加载动画**；网络错误 / 超时 / 鉴权失败分别给出可读错误提示。

### PR 切分与版本

- **PR-A**：网络与配置基座（引入 OkHttp；`AiClient` 接口 + OpenAI 兼容实现；设置页配置区；每日限额逻辑）
- **PR-B**：AI 助手页（三模式 + 上下文预填）+ 语法详情 / 错题本入口
- **PR-C**：收口 v1.1.0（versionName 1.1.0 / versionCode 22 + README 增补 + Release）
- 测试策略：Prompt 构建、响应解析、限额判定全部抽**纯函数单测**；网络层经 `AiClient` 接口抽象，单测用 fake 不打真网。每个 PR 照旧测试全绿交付（§17.8）。

## 19.10 v1.2 规划决策（2026-09-12 grilling 定案）

grilling 两轮问答确认，效力等同需求。后续实现如需偏离本节，先改本节再动代码。

### 主题：错题与 SRS 深度联动（两层）

- **错题突击会话**：复习 Tab 新增「错题突击」入口（副标题「待清错题 N 道」；错题本为空时卡片隐藏）；把错题本条目生成练习题——
  - **word** 错题：`QuizVariantPicker` 混合题型（选择/听音/汉字/打字），干扰项取全池
  - **kana** 错题：五十音 romaji 四选一（现有 `kanaQuiz`）
  - **grammar** 错题：该条语法自带练习题（`exercisesJson`）
  - 每轮 10 题、错题池洗牌循环取（不足 10 全出）；判分走 `recordAuxAnswer`——**答对即移出错题本，答错留下且次数 +1**；**不推进 SRS dueAt**（避免双算）
  - 结算展示「本轮答对 x/y · 剩余 N 道待清」
- **复习队列错题优先**：`dueWords` / `dueGrammar` 查询加 `LEFT JOIN wrong_answers` 排序（有错题的排前面，其余按 dueAt）——SQL 层纯排序改动，可回滚；不碰 FSRS 公式（19.6 纪律）。
- **统计口径不混**：突击会话时长照常 `addStudy`；突击对错**不进复习正确率**（那是 SRS 自评的指标）；错题维度统计等数据基建需求明确后再做（19.10 时不引入新表）。

### 搭车件：N4 词二批

- N4 单词 +300（N4 300 → 600，总词库 804 → 1104），走既有批次管线（`merge_words` + `validate_content` + 分类自动映射 unit），words version 升位自动重装、进度无损。

### 推迟项

- **流式输出**：等 AI 助手经过真实 Key 使用反馈后再决定（v1.1 刚交付，避免为时过早的优化）。
- **统计增强**：等错题联动落地、错题维度数据需求明确后再做。

### PR 切分与版本

- **PR-A**：错题突击会话（错题池编排纯函数 + 会话页 + 复习 Tab 入口）+ 复习队列错题优先 SQL
- **PR-B**：N4 词二批 +300
- **PR-C**：收口 v1.2.0（versionName 1.2.0 / versionCode 23 + README 增补 + Release）
- 顺序 A → B → C；PR-B 内容生产不阻塞 PR-A。每个 PR 照旧测试全绿交付（§17.8）。

## 19.11 v1.3 规划决策（2026-09-12 grilling 定案）

grilling 两轮问答确认，效力等同需求。**规划中途获得用户真机反馈（AI 回答质量还行、但等结果太久），主题据此从「统计增强」调整为「AI 流式输出」**——真实使用中的具体痛点优先于排期表。

### 主题：AI 流式输出

- 同一 `/chat/completions` 端点，请求体加 `"stream": true`；逐行读 SSE（`data: {...}` 增量块、`data: [DONE]` 结束），拼接 `choices[0].delta.content`。
- SSE 行解析抽**纯函数**（正常块 / DONE / 心跳注释行 / 残缺行均有单测）。
- UI：先显示「思考中…」，**首个字到达即开始打字机效果**（结果卡文本逐步增长）；流中断保留已收到的部分文本并附错误说明。
- **不做**「停止」按钮：离开页面即取消（ViewModel 作用域天然取消）。
- **调用计数时机：首个增量到达即计数**（等价于 200 响应头已收到、token 已在服务端消耗；中途取消不再额外扣减）。
- **Prompt 本版不动**：用户反馈质量「还行」且无具体抱怨，不做无样本盲调；攒够具体反馈再调。
- 前置修复：v1.2.1 热修复补声明 `INTERNET` 权限（AI 请求曾被系统整体拦截，见第 4 节踩坑）。

### 搭车件：N4 语法二批

- 语法 87 → ~120（N4 37 → ~70），N4 每单元语法从 3–4 条升至 6–7 条——课程化后语法是 N4 单元最薄的一层（词已 600）；每条带例句与配套练习，走批次管线 + 校验。

### 推迟与排期

- **统计增强 → v1.4 主题候选**（数据维度稳定，不会过期）。
- **汉字专项学习 → v1.4 候选**。
- 埋点继续推迟至内测。

### PR 切分与版本

- **PR-A**：流式管线（SSE 解析纯函数 + `AiClient.stream()` + 助手页打字机 + 计数时机）+ 单测
- **PR-B**：N4 语法二批
- **PR-C**：收口 v1.3.0（versionName 1.3.0 / versionCode 25 + README 增补 + Release）
- 每个 PR 照旧测试全绿交付（§17.8）。

## 19.12 应用内更新检查（2026-09-12 定案）

用户需求「每次发布新版本时给用户推送更新」。**明确不做 FCM 系统推送**：大陆无 GMS 设备收不到、需引入 Firebase + 服务端发推逻辑、无账号体系下 device token 管理为纯负担。采用**应用内检查更新**，形态与用户确认（方案 A）：

- **数据源 = GitHub Releases 本身，发版流程零改动**：启动后 GET `api.github.com/repos/Ageha6912/JapanLearn/releases/latest`，取 `tag_name` / `html_url` / `.apk` 资产直链；照常 `gh release create` 即可，无需维护版本清单文件。
- **节流**：每 24h（按日历日）最多查一次（`update_last_check_epoch_day`），查询前先落检查戳——当天网络失败也不会每次启动重试打 API。
- **失败完全静默**：网络不通 / GitHub 超时（3s+5s）/ 响应异常 → 无提示、无报错、无日志噪音；不产生任何新的「App 不可用」故障面。
- **提示形态**：首页可关闭横幅「新版本 vX.Y.Z 已发布」；「查看更新」跳系统浏览器打开 Release 页（用户自行下载 APK）；「跳过」记录 `update_skipped_version`，该版本不再提示。**第一版不做自动下载 + 唤起安装器**（需 REQUEST_INSTALL_PACKAGES + FileProvider + 厂商安装审核差异，收益不成比例）。
- **版本判定**：纯函数 semver 数值比较（缺失段补 0，两位数段按数值不按字符串），`versionCode` 之外的 tag 解析（`v` 前缀容忍，脏 tag 拒绝）。
- **隐私边界不变**：更新检查是设备发起的单向拉取，请求不携带任何用户数据（无 ID、无统计）；学习数据照旧不出设备；「完全离线可用」叙事保持成立——这是继 AI（19.9）之后第二个可选联网点，且无任何配置门槛。
- **已知局限与升级路径**：`api.github.com` 大陆可达性一般，静默兜底意味着大陆用户可能较少看到提示；若内测反馈集中，升级为方案 B——仓库内 `latest.json` + jsDelivr CDN（大陆可达性好），代价是发版清单多一步「更新 latest.json 并 push」。
- **实现**：`domain/UpdateChecker.kt` 纯函数（节流 / parseTag / isNewer / shouldPrompt / parseReleaseResponse）+ `data/update/GithubReleaseFetcher.kt`（短超时单向 GET）+ 首页横幅；判定逻辑 16 项单测。随 v1.4 发布，本版不发。

